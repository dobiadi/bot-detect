# Detect legacy Selenium

Older selenium versions put some of their internal objects to the global objects
which can be detected.

This doesn't really detect any bots nowadays but it is included nevertheless.
